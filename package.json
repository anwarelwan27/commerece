{
  "name": "technova-store",
  "version": "1.0.0",
  "description": "Full-stack electronics e-commerce store built with React, Express, and MySQL.",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "keywords": [
    "react",
    "express",
    "mysql",
    "ecommerce",
    "electronics-store"
  ],
  "author": "NASS Academy Student Project",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
